# Next.js build output
.next/
out/
next-env.d.ts

# Dependencies
node_modules/

# Runtime data — created on boot / by uploads. Never commit (contains user
# credentials hashes, sessions, and uploaded files).
storage/*
!storage/.gitkeep

# Local env & logs
.env
.env.local
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
